%h1 Parse websites
%br
= form_tag parse_websites_websites_path do
  = label_tag "Websites"
  %br
  =select_tag 'websites[]', options_for_select(@websites.map {|w| [w.name, w.id]}), :multiple => true, :size => 10, :style => "width:200px"
  %br
  = submit_tag "Submit", class: "btn btn-large btn-primary"

%h1 Listing websites

%table
  %thead
    %tr
      %th Name
      %th{:colspan => "3"}
  %tbody
    - @websites.each do |website|
      %tr
        %td= website.name
        %td= link_to 'Show', website
        %td= link_to 'Edit', edit_website_path(website)
        %td= link_to 'Destroy', website, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Website', new_website_path